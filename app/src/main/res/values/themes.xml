<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="ThemeDialog" parent="android:Theme.Holo.Dialog">
        <item name="android:windowMinWidthMajor">92%</item>
        <item name="android:windowMinWidthMinor">92%</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>


    <style name="NoFadeTheme" parent="Theme.Material3.DayNight.NoActionBar">  <!-- hoặc theme của mày -->
        <item name="android:windowAnimationStyle">@null</item>
        <!-- Hoặc dùng @android:style/Animation.NoAnimation nếu muốn explicit -->
        <item name="android:windowIsTranslucent">false</item>  <!-- tránh translucent fade -->
        <item name="android:windowBackground">@android:color/white</item>  <!-- hoặc màu nền chính -->
    </style>
</resources>